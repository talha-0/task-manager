function App() {
  return (
    <main className="app-shell">
      <section className="panel">
        <p className="eyebrow">Task Manager</p>
        <h1>Full-stack assignment scaffold</h1>
        <p className="lead">
          The React client and Express server are wired up. Task CRUD and persistence land in the
          next commits to keep the history readable.
        </p>
      </section>
    </main>
  );
}

export default App;
